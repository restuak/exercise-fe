export const metadata = {
  title: "Blog Preview",
  description: "Exercise: Blog Preview",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
